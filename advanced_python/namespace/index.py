#  In Python, Namespace represented a memory block where names are mapped to objects.
#
# - Class Namespace = A class maintains it's own namespace known as class namespace.
#                    In the class namespace, the names are mapped to class variables
#
# - Instance Namespace = Every instance have it's own namespace known as Instance namespace. In
#                        the Instance namespace, the names are mapped to instance variables.
#


    
