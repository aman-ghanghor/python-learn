# The variable which are declared inside a function called as Local Variable.
# Local variable scope is limited only to that function where it is created. It
# means local variable value is available only in that function not outside of that function.


def wish():
    name = "Guest"
    print(f"Hello {name}")

wish()

# print(name)                         # Not accessing outside the function


















































