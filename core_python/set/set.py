# Sets is collection
# 1. Unordered so, we can not access element using index
# 2. No duplicate Items
# 3. mutable so, we can modify


myset1 = {"banana", "apple", "cherry", "apple", "apple"}
myset2 = set({"gobhi", "bhindi", "Matar"})
myset3 = set()
myset4 = {(1, 2, 3), 'apple', 'banana'}

print(myset1)
print(myset2)
print("")

# Accessing set
for x in myset1:
    print(x)










